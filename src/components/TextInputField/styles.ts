import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textBox: {
    flex: 1,
    fontSize: 16,
    backgroundColor: "#333333",
    color: "#fff",
    padding: 16,
    borderRadius: 8,
    borderColor: "#0D0D0D",
  },
});
